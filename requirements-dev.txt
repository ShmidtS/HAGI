# HAGI prototype — development dependencies
-r requirements.txt

ruff>=0.5            # format + lint
pytest>=8.0
pytest-cov>=5.0
hypothesis>=6.100    # property-based tests (mirror Lean invariants)
pip-audit>=2.7       # dependency CVE scan
