"""Data loading and processing.

TODO (Stage 0):
  - tokenizer wrapper (reuse existing BPE, e.g. Llama-3.2 tokenizer)
  - streaming shard loader for FineWeb-Edu + code + math mix
  - PrefixLM packing (prefix bidirectional + response causal)
  - CoT distillation data ingestion (Stage 0 phase 2)
"""
