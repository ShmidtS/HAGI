# HAGI — runtime dependencies
# Pin minor versions for reproducibility; update deliberately.

# Core
torch>=2.2
numpy>=1.26
pyyaml>=6.0
typer>=0.12
pexpect>=4.9

# Data
transformers>=4.40
datasets>=2.19
tokenizers>=0.19
safetensors>=0.4

# Eval (optional: pip install lm-eval or pip install -e ".[lm-eval]")
lm-eval>=0.4           # EleutherAI harness — GSM8K/ARC/BoolQ/...

# Experiment tracking (optional, gated by config)
wandb>=0.17
