# Executed run records

Committed **as-executed** notebooks (with outputs) from cloud runs — the raw
provenance behind the results in `docs/`. Download from Kaggle/Colab via
*File → Download Notebook* after a run and commit here with a dated name:

    hagi_kaggle_geo_diag_2026-06-09.ipynb

Summary records live in `docs/` (e.g. `docs/GEO_DIAG.md`); these files are the
unedited evidence. Do not re-run notebooks in this directory — re-run the clean
templates in `notebooks/` instead.
